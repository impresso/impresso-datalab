---
import { AstroSeo } from '@astrolib/seo';
import SearchQuerySnippetModal from '../components/SearchQuerySnippetModal';
import Layout from '../layouts/Layout.astro';
import { CanonicalUrl } from '../constants';

const title = 'Draft Notebook - Impresso Datalab';
const description = 'Explore and work programmatically with the Impresso Corpus, Data and Models.';

---

<Layout 
	title={title}
	description={description}
>
<AstroSeo
	title={title}
	description={description}
	canonical={CanonicalUrl}
/>
	<SearchQuerySnippetModal client:only="react"/>
</Layout>
